<Page
    x:Class="JpnExamCommando.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JpnExamCommando"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot x:Name="Verbs">
            <Pivot.HeaderTemplate>
                <DataTemplate x:DataType="local:VocabularyCourse">
                    <TextBlock Text="{x:Bind md}"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <Pivot.ItemTemplate>
                <DataTemplate x:DataType="local:VocabularyCourse">
                    <GridView ItemsSource="{x:Bind Content}">
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="local:VocabularyItem">
                                
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>
    </Grid>
</Page>
